<%- include('../includes/head', { title: "Login" }) %>

<div class="fixed inset-0 bg-gray-100 flex items-center justify-center z-50" id="loginPage">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-sm">
      <h2 class="text-xl font-bold mb-4 text-center">Connexion</h2>
      <form class="space-y-4">
        <div>
          <input type="email" placeholder="Adresse e-mail" class="border px-3 py-2 rounded w-full text-sm" />
          <p class="text-xs text-red-500 mt-1">Email requis</p>
        </div>
        <div>
          <input type="password" placeholder="Mot de passe" class="border px-3 py-2 rounded w-full text-sm" />
          <p class="text-xs text-red-500 mt-1">Mot de passe requis</p>
        </div>
        <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded w-full text-sm hover:bg-blue-700">Se connecter</button>
        <a href="/index" class="bg-blue-600 text-white px-4 py-2 rounded w-full text-sm hover:bg-blue-700">Se connecter - test</a>
      </form>
      <p class="text-sm text-center mt-4">Pas de compte ? <a href="/auth/signup" class="text-blue-600 hover:underline">S'inscrire</a></p>
    </div>
  </div>

<%- include('../includes/end.ejs') %>